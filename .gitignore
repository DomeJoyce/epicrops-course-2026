# Downloaded / generated data — reproducible via the scripts, too big for git
data/raw/**
!data/raw/**/.gitkeep
data/reference/**
!data/reference/**/.gitkeep

# Analysis outputs
results/**
!results/**/.gitkeep

# Precomputed fallbacks are distributed via Docker Hub / release assets, not git
precomputed/

# Instructor-only handoff notes (never publish to the student repo)
HANDOFF.md

# Cruft
*.tar.gz
*.bam
*.bai
*.fastq
*.fastq.gz
*.fq
*.log
.ipynb_checkpoints/
__pycache__/
*.pyc
.DS_Store
.Rhistory
